# Pseudocode

#### Add the steps you need to take to implement the requirements for Scrabble Challenge

```
1. Create a variable named score and set it to zero.

2. Create an object called letterValues, in which list the letter : value key pairs.

3. Re-set the word variable to upercase.

4. Create a for loop to iterate through the letters in the word and add the value pairs of the letters to the score variable.

5. Add an if statement within the function, to define that if the word variable is not a string or not provided than the result is 0 
...etc
```
